<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:padding="16dp"
    android:layout_height="match_parent">
    <TextView
        android:id="@+id/jobTitle"
        android:layout_width="250dp"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:textColor="#1825ba"
        android:textSize="16sp"
        tools:text="Human Resources office assistant"
        style="@style/Base.TextAppearance.AppCompat.Body1"
        android:textStyle="bold" />
    <TextView
        android:id="@+id/department"
        android:layout_below="@id/jobTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:layout_alignStart="@id/jobTitle"
        tools:text="Computer Science"
        android:layout_marginTop="4dp" />
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id="@+id/deleteJob"
        android:drawableEnd="@drawable/ic_clear_black_18dp"
        style="?android:attr/buttonBarButtonStyle"
        android:layout_alignParentEnd="true"
        android:layout_toEndOf="@id/jobTitle" />
</RelativeLayout>